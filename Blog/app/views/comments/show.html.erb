<p id="notice"><%= notice %></p>

<p>
  <strong>Body:</strong>
  <h3><%= @comment.body %><h3>
</p>

<%= link_to 'Edit', edit_post_comment_path(@comment.post,@comment) %> |
<%= link_to 'Back', post_comments_path(@comment.post,@comment) %>
